import { Link } from 'react-router';

const Navbar = () => {
  return (
    <div>
      <Link to="/">Home</Link>
      <Link to="/login">Home</Link>
      <Link to="/sign-up">Home</Link>
    </div>
  );
};

export default Navbar;
